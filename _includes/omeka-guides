{% assign omeka_posts = site.posts | where: "categories", "omeka" | where: "categories", "guides" %}

{% if omeka_posts.size > 0 %}
<ul>
  {% for post in omeka_posts %}<li><a href="{{ site.baseurl }}{{ post.url }}">{{ post.title }}</a></li>{% endfor %}
</ul>
{% endif %}